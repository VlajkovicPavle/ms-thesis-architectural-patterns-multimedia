#!/usr/bin/env sh
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
ROOT_DIR="$(CDPATH= cd -- "$SCRIPT_DIR/../.." && pwd)"

exec "$ROOT_DIR/backend/media-monolith/mvnw" -f "$SCRIPT_DIR/pom.xml" "$@"
